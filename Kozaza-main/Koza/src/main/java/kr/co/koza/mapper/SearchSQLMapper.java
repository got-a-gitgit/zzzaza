package kr.co.koza.mapper;

import java.util.ArrayList;

import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;

public interface SearchSQLMapper {
	
//	@Select("SELECT * FROM Movie WHERE ${searchTarget} LIKE #{searchWord}")
//	public ArrayList<MovieVO> searchByOneWord(@Param("searchTarget")String searchTarget, @Param("searchWord")String searchWord);
	
	
	
}
